#!/usr/bin/env bash

# install GNU core utils, can be used with 'g' prefix.
# e.g. 'gln' instead of 'ln' lets us use -r option.
brew install coreutils

# etc
brew install ffmpeg
brew install tmux
brew install wget
brew install youtube-dl

